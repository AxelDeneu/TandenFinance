# Public URL, used for OG Image when running nuxt generate
NUXT_PUBLIC_SITE_URL=

# Bearer token required to call the MCP endpoint at /api/mcp
# Generate a long random string and share it with the MCP client (e.g. Hermes Agent)
NUXT_MCP_TOKEN=
